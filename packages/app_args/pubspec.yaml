name: tekartik_app_args
description: Args helper
version: 0.1.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  args: ">=2.7.0"
  # path: ^1.8.0

dev_dependencies:
  lints: ">=6.1.0"
  test: ">=1.31.0"
  tekartik_lints:
    git:
      url: https://github.com/tekartik/common.dart
      path: packages/lints
    version: '>=0.1.0'
resolution: workspace
