/// Launch terminal options.
class LaunchTerminalOptions {
  /// The directory path where the terminal should be opened.
  final String? directoryPath;

  /// Creates launch terminal options.
  LaunchTerminalOptions({this.directoryPath});
}
