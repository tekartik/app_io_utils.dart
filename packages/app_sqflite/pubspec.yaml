name: tekartik_app_io_sqflite
description: Sqflite common initialization for io app
version: 0.5.0
publish_to: none

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  sqflite_common_ffi: '>=2.0.0'
  sqflite_common: '>=2.0.0+1'

dev_dependencies:
  pedantic:
  process_run:
  test:
  dev_test:
  tekartik_lints:
    git:
      url: git://github.com/tekartik/common.dart
      ref: null_safety
      path: packages/lints
    version: '>=0.1.0'

dependency_overrides:
